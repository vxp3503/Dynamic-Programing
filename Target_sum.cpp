#include<bits/stdc++.h>
using namespace std;
int Target_sum(int arr[],int n)
{
    
}
int main()
{
    cout<<"Enter the no. of elements";
    int n;
    cin>>n;
    cout<<"Enter the elements";
    int arr[n];
    for(int i=0;i<n;i++)
    {
        cin>>arr[i];
    }
    cout<<Target_sum(arr,n);
}